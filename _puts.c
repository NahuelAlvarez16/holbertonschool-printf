#include "main.h"
/**
 * _puts - Prints a string
 * @src: Contains
 * Return: The length of @src
 */
int _puts(char *src)
{
	int i;

	for (i = 0; src && scr[i], i++)
		_putchar(scr[i]);

	return (i);
}
